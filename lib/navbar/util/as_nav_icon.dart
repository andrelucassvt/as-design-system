import 'package:flutter/material.dart';

class AsNavIcon {
  final String? title;
  final Widget icon;
  final VoidCallback onTap;
  AsNavIcon({this.title, required this.icon, required this.onTap});
}
